File test.md

# Markdown exa;ple

Note how we can correctly syntax-highlight the code blocs *inside* the Markdown document.

Python example:

```python
cmd = "bat"
print("Hello from {}".format(cmd))

Ruby example:

```ruby
cmd = "bat"
puts "Hello from #{cmd}"

